[pytest]
# Only collect OUR tests -- never crawl into site-packages / numpy internals,
# even if a virtualenv was created inside the project directory.
testpaths = tests
addopts = -q
norecursedirs = venv .venv env lib lib64 bin include build dist *.egg-info
