# --- core dev (mock-based development runs on this alone) ---
pytest>=7.0
bitsandbytes==0.49.2
cairosvg==2.9.0
librosa==0.11.0
matplotlib==3.11.1
moshi==0.2.13
numpy==2.5.2
scikit-learn==1.9.0
soundfile==0.14.0
sphn==0.2.1

# torch is installed separately for the right CUDA build -- see README Setup step 2:
#   pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
# torch==2.9.1